# API base URL for browser requests.
# Keep empty to use same-origin calls (recommended with Vite proxy in dev).
VITE_API_BASE_URL=

# Dev proxy target for /api -> backend server.
# Example: http://127.0.0.1:8000
VITE_API_PROXY_TARGET=http://127.0.0.1:8000
