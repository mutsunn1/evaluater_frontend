

**Role:** 你现在是一位经验丰富的前端架构师和高级开发工程师。请帮我使用Vue3 和 TailwindCSS 构建一个“动态交互式中文评测（HSK）”的核心答题页面。

**Context:**
我们的后端使用的是 FastAPI，采用“RESTful 状态控制 + SSE 流式推送”的混合架构。目前后端 API 契约已经就绪。这个页面的核心难点在于：**服务端权威计时（防作弊）**、**页面可见性监听（切屏检测）**以及**处理长连接（SSE）的数据流转**。

**Backend API Contract (FastAPI):**
1. **启动评测:** `POST /api/v2/evaluations/start` (返回 `session_id`, `server_timestamp_ms` 以及第一道题目的 `expected_time_ms` 和 `hard_timeout_ms`)
2. **切屏/暂停:** `POST /api/v2/evaluations/{session_id}/timer/pause` (需携带 reason: "tab_switch" 或 "user_pause")
3. **恢复计时:** `POST /api/v2/evaluations/{session_id}/timer/resume`
4. **心跳保活:** `PUT /api/v2/evaluations/{session_id}/timer/heartbeat` (每 5 秒一次)
5. **提交答案:** `POST /api/v2/evaluations/{session_id}/questions/{q_id}/submit` (异步接口，返回 202 Accepted)
6. **题目/状态流:** `GET /api/v2/evaluations/{session_id}/stream` (SSE 接口，监听 "new_question" 和 "evaluation_complete" 事件)

**Requirements & Tasks:**

**1. 核心 Hook/Composable 设计 (逻辑层分离):**
请不要把所有逻辑塞在 UI 组件里，务必抽离出以下几个独立逻辑：
* `useServerTimer`: 处理前后端时间偏差校准。基于后端下发的 `hard_timeout_ms` 进行本地倒计时渲染。如果倒计时归零，自动触发交卷逻辑。
* `useVisibilityTracker`: 监听浏览器的 `visibilitychange` 事件。当用户切屏（`document.hidden`）时，自动调用后端的 `/pause` 接口；切回时调用 `/resume` 接口。
* `useEvaluationStream`: 负责建立和维护与 `/stream` 接口的 SSE (`EventSource`) 连接，并提供响应式的状态（如 `currentQuestion`, `isAnalyzing`, `isComplete`）。

**2. UI 组件交互流 (UI 流转):**
* **加载状态:** 用户点击“开始评测”时，显示全屏骨架屏，调用 `/start` 并建立 SSE 连接。
* **答题状态:** 顶部显示倒计时进度条。倒计时根据 `expected_time_ms` 显示绿色，超过预期但未到 `hard_timeout_ms` 显示橙色警告。
* **提交与过渡:** 用户点击提交后，UI 进入“Agent 分析中...”的动效（禁用答题区）。
* **流式接收:** 监听到 SSE 的 `new_question` 事件后，以类似“打字机”的动画渲染新题目，结束分析动效；监听到 `evaluation_complete` 后，跳转或渲染最终报告组件。

**3. 异常处理 (Edge Cases):**
* 处理 SSE 连接断开重连机制。
* 如果心跳请求 (`/heartbeat`) 连续 3 次失败，提示用户网络异常并暂停本地计时。

**Output Format:**
1.  **目录结构建议:** 列出该功能模块所需的文件结构。
2.  **核心逻辑代码:** 提供上述 3 个核心 Hook（状态管理与 API 请求）的完整 TypeScript 代码。
3.  **UI 组件代码:** 提供主答题页面（`EvaluationBoard`）的代码，使用 TailwindCSS 进行美观的现代化布局，体现出加载过渡和答题区的层次感。

